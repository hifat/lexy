# Shell

- Check running process on port
```bash
   lsof -i :<port>
```

- Kill process
```bash
   kill <PID>

   # or

   pkill <app_name>
```

- Generate base64 string
```bash
   openssl rand -base64 <length_number>
```